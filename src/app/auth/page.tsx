import { AuthPage } from "@/components/auth";

export const metadata = {
  title: "Login | Observer.GG",
  description: "Entre na sua conta Observer.GG ou crie uma nova para começar a evoluir seu gameplay.",
};

export default function Auth() {
  return <AuthPage />;
}
